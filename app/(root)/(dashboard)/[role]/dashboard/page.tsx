import OverViews from '@/app/(root)/dashboard/components/OverViews';

const page = () => {
  return (
    <section className="container">
      <OverViews />
    </section>
  );
};

export default page;
